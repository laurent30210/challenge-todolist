import React from 'react';

// import CSS
import './Form.scss';

const Form = () => (  
    <form className="form">
       <input
        className="form-input"
        type="text"
        placeholder="Indiquer votre tache ici"
      />
    </form>
)

export default Form;
